+00 ptr  next node in linked list
+04 ptr  owning queue
+08 word relative offset to locate node's payload or data section
+0C word flag / status or type indicator (e.g. 4)

----
The node is part of a larger container structure, aligned to 0x100 boundaries (node & 0xFFFFFF00).
